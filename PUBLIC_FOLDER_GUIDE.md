# 如何添加 GLB 3D 模型

## 方法一：使用 public 文件夾（推薦）

如果你在本地開發環境：

1. 在項目根目錄創建 `public` 文件夾（如果還沒有的話）
2. 將你的 `plant.glb` 文件放入 `public` 文件夾
3. 文件結構應該是：
```
your-project/
├── public/
│   └── plant.glb
├── src/
└── ...
```

4. 在 `App.tsx` 中，模型會通過 `/plant.glb` 路徑自動加載

## 方法二：在瀏覽器中直接上傳

如果你無法訪問文件系統或想快速測試：

1. 打開應用程式
2. 在首頁中間的佔位符上，懸停鼠標會看到「上傳 GLB 模型」按鈕
3. 點擊按鈕並選擇你的 `.glb` 文件
4. 模型會立即加載並顯示
5. 右下角有一個上傳圖標，可以隨時更換模型

## 關於時間主題

你的 3D 角色會根據當前時間自動調整光照和背景：

- **早晨 (5:00-10:00)**: 淺藍色背景 (#EAF4FF)，柔和光線
- **白天 (10:00-16:00)**: 米色背景 (#F5F3F0)，明亮光線
- **傍晚 (16:00-19:00)**: 暖橙色背景 (#FFE8D6)，溫暖光線
- **夜晚 (19:00-5:00)**: 深藍灰背景 (#0F172A)，柔和月光

## 技術細節

- 模型會自動縮放以適應螢幕
- 支持滑鼠拖曳旋轉（已禁用縮放和平移）
- 模型會有輕微的上下漂浮動畫
- 使用 Three.js 和 OrbitControls 實現

## 注意事項

- 建議 GLB 文件大小在 5MB 以下，以確保快速加載
- 確保模型在導出時已經優化（移除不必要的材質和貼圖）
- 如果模型顯示不正確，檢查模型的軸向和縮放比例
